<UserControl x:Class="LogisticsApp.Views.LogViewerPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100">

    <b:Interaction.Triggers>
        <b:EventTrigger EventName="Loaded">
            <b:InvokeCommandAction Command="{Binding InitializeCommand}"/>
        </b:EventTrigger>
    </b:Interaction.Triggers>

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <!-- Стили для красивых бейджей статусов логов -->
        <Style x:Key="LogLevelBadgeBorderStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="10,4"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Level}" Value="ERR">
                    <Setter Property="Background" Value="#FEE2E2"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Level}" Value="FTL">
                    <Setter Property="Background" Value="#DC2626"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Level}" Value="WRN">
                    <Setter Property="Background" Value="#FEF3C7"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Level}" Value="INF">
                    <Setter Property="Background" Value="#DBEAFE"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Level}" Value="DBG">
                    <Setter Property="Background" Value="#F3F4F6"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="LogLevelBadgeTextStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Level}" Value="ERR">
                    <Setter Property="Foreground" Value="#DC2626"/>
                    <Setter Property="Text" Value="ERROR"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Level}" Value="FTL">
                    <Setter Property="Foreground" Value="#FFFFFF"/>
                    <Setter Property="Text" Value="FATAL"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Level}" Value="WRN">
                    <Setter Property="Foreground" Value="#D97706"/>
                    <Setter Property="Text" Value="WARN"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Level}" Value="INF">
                    <Setter Property="Foreground" Value="#2563EB"/>
                    <Setter Property="Text" Value="INFO"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Level}" Value="DBG">
                    <Setter Property="Foreground" Value="#4B5563"/>
                    <Setter Property="Text" Value="DEBUG"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ШАПКА -->
        <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" Padding="20" BorderThickness="0,0,0,1" BorderBrush="{StaticResource BorderBrush}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" Grid.Column="0" VerticalAlignment="Center">
                    <Border Width="44" Height="44" Background="{StaticResource PrimaryLightBrush}" CornerRadius="10">
                        <iconPacks:PackIconMaterial Kind="Console" Foreground="{StaticResource PrimaryDarkBrush}" VerticalAlignment="Center" HorizontalAlignment="Center" Width="22" Height="22"/>
                    </Border>
                    <StackPanel Margin="15,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="Системный Журнал (Логи)" FontWeight="Bold" FontSize="18" Foreground="{StaticResource TextPrimaryBrush}"/>
                        <TextBlock Text="Диагностика и анализ событий приложения" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                    </StackPanel>
                </StackPanel>
                <Button Grid.Column="2" Command="{Binding TriggerLogReloadCommand}" Style="{StaticResource MenuButton}" ToolTip="Обновить" Height="44" Padding="20,0">
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Kind="Refresh" Width="18" Height="18" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,8,0"/>
                        <TextBlock Text="ОБНОВИТЬ" FontWeight="Bold" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <!-- ПАНЕЛЬ ФИЛЬТРОВ (Отзывчивая через WrapPanel) -->
        <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" Padding="20,20,20,5" Margin="0,15,0,0" CornerRadius="8" BorderThickness="1" BorderBrush="{StaticResource BorderBrush}">
            <WrapPanel Orientation="Horizontal" VerticalAlignment="Center">

                <StackPanel Margin="0,0,25,15">
                    <TextBlock Text="Файл журнала" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                    <ComboBox ItemsSource="{Binding AvailableFiles}" SelectedItem="{Binding SelectedFile}" Width="240" Height="40" mah:TextBoxHelper.ClearTextButton="False" Style="{StaticResource MahApps.Styles.ComboBox}"/>
                </StackPanel>

                <StackPanel Margin="0,0,25,15">
                    <TextBlock Text="Уровень важности" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                    <ComboBox ItemsSource="{Binding LogLevels}" SelectedItem="{Binding SelectedLogLevel}" Width="160" Height="40" mah:TextBoxHelper.ClearTextButton="False" Style="{StaticResource MahApps.Styles.ComboBox}"/>
                </StackPanel>

                <StackPanel Margin="0,0,25,15" MinWidth="350">
                    <TextBlock Text="Поиск (текст или ошибка)" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Height="40" mah:TextBoxHelper.Watermark="Начните вводить текст для фильтрации..." mah:TextBoxHelper.ClearTextButton="True" Style="{StaticResource ModernTextBox}"/>
                </StackPanel>

                <Button Command="{Binding ClearFiltersCommand}" Style="{StaticResource MenuButton}" Height="40" Margin="0,0,0,15" VerticalAlignment="Bottom" ToolTip="Сбросить все фильтры">
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Kind="FilterRemoveOutline" Width="18" Height="18" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="#D32F2F"/>
                        <TextBlock Text="СБРОСИТЬ ФИЛЬТРЫ" Foreground="#D32F2F" FontWeight="Bold" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </WrapPanel>
        </Border>

        <!-- ТАБЛИЦА ЛОГОВ -->
        <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}" CornerRadius="8" BorderThickness="1" BorderBrush="{StaticResource BorderBrush}" Margin="0,15,0,0">
            <Grid>
                <DataGrid ItemsSource="{Binding Logs}" 
                          AutoGenerateColumns="False" 
                          IsReadOnly="True" 
                          HeadersVisibility="Column" 
                          SelectionMode="Single"
                          VirtualizingPanel.IsVirtualizing="True" 
                          VirtualizingPanel.VirtualizationMode="Recycling" 
                          ScrollViewer.CanContentScroll="True"
                          ScrollViewer.HorizontalScrollBarVisibility="Auto"
                          RowDetailsVisibilityMode="VisibleWhenSelected" 
                          Margin="10">

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Дата и Время" Binding="{Binding Timestamp, StringFormat=yyyy-MM-dd HH:mm:ss.fff}" Width="170" Foreground="#6B7280" FontWeight="SemiBold"/>

                        <!-- Обновленный столбец с красивыми бейджами -->
                        <DataGridTemplateColumn Header="Уровень" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource LogLevelBadgeBorderStyle}">
                                        <TextBlock Style="{StaticResource LogLevelBadgeTextStyle}"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTextColumn Header="Текст сообщения" Binding="{Binding Message}" Width="*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                    <Setter Property="ToolTip" Value="{Binding Message}"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Margin" Value="5,0,0,0"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Индикатор наличия Exception -->
                        <DataGridTemplateColumn Header="Исключение" Width="110">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Visibility="{Binding HasException, Converter={StaticResource BoolToVis}}">
                                        <iconPacks:PackIconMaterial Kind="AlertCircle" Foreground="#EF4444" Width="16" Height="16" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                        <TextBlock Text="Детали" Foreground="#EF4444" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>

                    <!-- Развернутая информация (Stack Trace) -->
                    <DataGrid.RowDetailsTemplate>
                        <DataTemplate>
                            <Border Background="#F8FAFC" Padding="20" Margin="10,5,10,15" CornerRadius="6" BorderThickness="1" BorderBrush="#E2E8F0">
                                <StackPanel>
                                    <TextBlock Text="Полное сообщение:" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" FontWeight="SemiBold" Margin="0,0,0,6"/>
                                    <TextBlock Text="{Binding Message}" TextWrapping="Wrap" Foreground="{StaticResource TextPrimaryBrush}" FontSize="14" Margin="0,0,0,20"/>

                                    <StackPanel Visibility="{Binding HasException, Converter={StaticResource BoolToVis}}">
                                        <TextBlock Text="Стек вызовов (Exception Details):" FontSize="13" FontWeight="Bold" Foreground="#DC2626" Margin="0,0,0,8"/>
                                        <Border Background="#1E293B" CornerRadius="6" Padding="10">
                                            <TextBox Text="{Binding ExceptionDetails}" 
                                                     IsReadOnly="True" 
                                                     TextWrapping="Wrap" 
                                                     Background="Transparent" 
                                                     Foreground="#F8FAFC" 
                                                     BorderThickness="0"
                                                     FontFamily="Consolas" 
                                                     FontSize="12"
                                                     MaxHeight="350" 
                                                     VerticalScrollBarVisibility="Auto"/>
                                        </Border>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </DataGrid.RowDetailsTemplate>
                </DataGrid>

                <!-- КРАСИВЫЙ ОВЕРЛЕЙ ЗАГРУЗКИ (Glassmorphism) -->
                <Border Background="#CCFFFFFF" CornerRadius="8" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <mah:ProgressRing IsActive="True" Foreground="{StaticResource PrimaryBrush}" Width="60" Height="60" Margin="0,0,0,15"/>
                        <TextBlock Text="Чтение логов..." FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Center"/>
                        <TextBlock Text="Пожалуйста, подождите" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>