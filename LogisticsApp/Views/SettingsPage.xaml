<UserControl x:Class="LogisticsApp.Views.SettingsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1100">
    <Grid Margin="20">
        <Border Background="{DynamicResource MahApps.Brushes.ThemeBackground}" 
                BorderBrush="{DynamicResource MahApps.Brushes.Control.Border}" 
                BorderThickness="1" 
                CornerRadius="8">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="15" ShadowDepth="2" Opacity="0.05"/>
            </Border.Effect>
            <TabControl mah:TabControlHelper.Underlined="TabPanel" Margin="10">

                <!-- ВКЛАДКА 1: ОБЩИЕ ПАРАМЕТРЫ -->
                <TabItem Header="Общие параметры" mah:HeaderedControlHelper.HeaderFontSize="16">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="20">
                            <TextBlock Text="Персонализация и Интерфейс" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>
                            <Grid Margin="0,0,0,25">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="280"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Акцентный цвет (Корпоративный):" VerticalAlignment="Center" FontSize="14" FontWeight="SemiBold"/>
                                <ComboBox Grid.Column="1" ItemsSource="{Binding AvailableAccents}" SelectedItem="{Binding SelectedAccent}" Width="200" HorizontalAlignment="Left" Style="{StaticResource MahApps.Styles.ComboBox}"/>
                            </Grid>

                            <Separator Margin="0,10,0,20" Background="{DynamicResource MahApps.Brushes.Control.Border}"/>

                            <TextBlock Text="Политики и Безопасность" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>
                            <Grid Margin="0,0,0,20">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="280"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Тайм-аут сессии (минуты):" VerticalAlignment="Center" FontSize="14" FontWeight="SemiBold"/>
                                <mah:NumericUpDown Grid.Column="1" Value="{Binding SessionTimeoutMinutes}" Minimum="5" Maximum="240" Width="150" HorizontalAlignment="Left"/>
                            </Grid>

                            <Separator Margin="0,10,0,20" Background="{DynamicResource MahApps.Brushes.Control.Border}"/>

                            <TextBlock Text="Бизнес-правила Логистики" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>
                            <Grid Margin="0,0,0,20">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="280"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Допустимый перегруз ТС (%):" VerticalAlignment="Center" FontSize="14" FontWeight="SemiBold"/>
                                <mah:NumericUpDown Grid.Column="1" Value="{Binding MaxOverloadPercentage}" Minimum="0" Maximum="20" StringFormat="F1" Width="150" HorizontalAlignment="Left"/>
                            </Grid>
                            <Grid Margin="0,0,0,20">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="280"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Упреждение сроков документов (дни):" VerticalAlignment="Center" FontSize="14" FontWeight="SemiBold"/>
                                <mah:NumericUpDown Grid.Column="1" Value="{Binding DocumentExpiryWarningDays}" Minimum="1" Maximum="60" Width="150" HorizontalAlignment="Left"/>
                            </Grid>
                            <Grid Margin="0,0,0,20">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="280"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Строгий контроль санобработки:" VerticalAlignment="Center" FontSize="14" FontWeight="SemiBold"/>
                                <mah:ToggleSwitch Grid.Column="1" IsOn="{Binding StrictSanitizationCheck}" OffContent="Отключен" OnContent="Включен" FontSize="14"/>
                            </Grid>
                            <Grid Margin="0,0,0,20">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="280"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Автообновление дашборда:" VerticalAlignment="Center" FontSize="14" FontWeight="SemiBold"/>
                                <mah:ToggleSwitch Grid.Column="1" IsOn="{Binding AutoRefreshDashboard}" OffContent="Вручную" OnContent="Автоматически" FontSize="14"/>
                            </Grid>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

                <!-- ВКЛАДКА 2: API И ИНТЕГРАЦИИ -->
                <TabItem Header="API и Интеграции" mah:HeaderedControlHelper.HeaderFontSize="16">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="20">
                            <TextBlock Text="DaData.ru (Реестр ФНС и Геокодирование)" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>
                            <TextBlock Text="Используется для автозаполнения контрагентов по ИНН и расчета расстояний." Foreground="Gray" Margin="0,0,0,15"/>
                            <TextBlock Text="API Ключ (Token):" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding DaDataApiKey, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" Margin="0,0,0,25" Width="450" HorizontalAlignment="Left"/>

                            <Separator Margin="0,10,0,20" Background="{DynamicResource MahApps.Brushes.Control.Border}"/>

                            <TextBlock Text="Картография (GMap.NET)" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>
                            <Grid Margin="0,0,0,20">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="250"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Провайдер карт:" VerticalAlignment="Center" FontSize="14" FontWeight="SemiBold"/>
                                <ComboBox Grid.Column="1" ItemsSource="{Binding AvailableMapProviders}" SelectedItem="{Binding MapProvider}" Width="200" HorizontalAlignment="Left" Style="{StaticResource MahApps.Styles.ComboBox}"/>
                            </Grid>
                            <Grid Margin="0,0,0,25">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="250"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Масштаб карты по умолчанию:" VerticalAlignment="Center" FontSize="14" FontWeight="SemiBold"/>
                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                    <mah:NumericUpDown Value="{Binding DefaultMapZoom}" Minimum="5" Maximum="18" Width="100"/>
                                    <TextBlock Text="(от 5 до 18)" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="Gray"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

                <!-- ВКЛАДКА 3: СИСТЕМА И БАЗА ДАННЫХ -->
                <TabItem Header="Система и База данных" mah:HeaderedControlHelper.HeaderFontSize="16">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="20">
                            <!-- Оригинальный блок логов и БД -->
                            <TextBlock Text="Диагностика и Логирование" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>
                            <Grid Margin="0,0,0,20">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="250"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Уровень логирования (Serilog):" VerticalAlignment="Center" FontSize="14" FontWeight="SemiBold"/>
                                <ComboBox Grid.Column="1" ItemsSource="{Binding AvailableLogLevels}" SelectedItem="{Binding LogLevel}" Width="200" HorizontalAlignment="Left" Style="{StaticResource MahApps.Styles.ComboBox}"/>
                            </Grid>
                            <Grid Margin="0,0,0,25">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="250"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Глубина хранения аудита (дни):" VerticalAlignment="Center" FontSize="14" FontWeight="SemiBold"/>
                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                    <mah:NumericUpDown Value="{Binding AuditRetentionDays}" Minimum="10" Maximum="365" Width="100"/>
                                    <TextBlock Text="дней" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="Gray"/>
                                </StackPanel>
                            </Grid>
                            <Grid Margin="0,0,0,25">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="250"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Срок хранения в Корзине:" VerticalAlignment="Center" FontSize="14" FontWeight="SemiBold"/>
                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                    <mah:NumericUpDown Value="{Binding ArchiveRetentionDays}" Minimum="1" Maximum="365" Width="100"/>
                                    <TextBlock Text="дней (до полного удаления)" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="Gray"/>
                                </StackPanel>
                            </Grid>

                            <Separator Margin="0,10,0,20" Background="{DynamicResource MahApps.Brushes.Control.Border}"/>

                            <TextBlock Text="Подключение к SQL Server" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
                            <TextBlock Text="Внимание: Изменяйте адрес сервера только если база данных была перенесена на другой компьютер." Foreground="#D32F2F" Margin="0,0,0,20" TextWrapping="Wrap"/>
                            <TextBlock Text="Имя сервера (Server):" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding DbServerName, UpdateSourceTrigger=PropertyChanged}" 
                                     mah:TextBoxHelper.Watermark="Например: .\SQLEXPRESS или (localdb)\mssqllocaldb"
                                     Style="{StaticResource ModernTextBox}"
                                     Margin="0,0,0,5" Width="450" HorizontalAlignment="Left"/>
                            <TextBlock Text="Остальные параметры скрыты и зафиксированы системой для безопасности." Foreground="Gray" FontSize="12" Margin="0,0,0,20"/>

                            <StackPanel Orientation="Horizontal" Margin="0,0,0,30">
                                <Button Command="{Binding TestConnectionCommand}" Style="{StaticResource MenuButton}" Margin="0,0,10,0">
                                    <StackPanel Orientation="Horizontal">
                                        <iconPacks:PackIconMaterial Kind="DatabaseCheck" Margin="0,0,5,0" Foreground="#2E7D32"/>
                                        <TextBlock Text="ПРОВЕРИТЬ СВЯЗЬ" Foreground="#2E7D32"/>
                                    </StackPanel>
                                </Button>
                                <Button Content="СОХРАНИТЬ СТРОКУ" Command="{Binding SaveDbSettingsCommand}" Style="{StaticResource PrimaryButton}"/>
                            </StackPanel>

                            <TextBlock Text="Резервное копирование" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
                            <Button Command="{Binding BackupDatabaseCommand}" Style="{StaticResource MenuButton}" HorizontalAlignment="Left">
                                <StackPanel Orientation="Horizontal">
                                    <iconPacks:PackIconMaterial Kind="DatabaseExport" Margin="0,0,5,0"/>
                                    <TextBlock Text="СОЗДАТЬ РЕЗЕРВНУЮ КОПИЮ (.BAK)"/>
                                </StackPanel>
                            </Button>

                            <Separator Margin="0,30,0,20" Background="{DynamicResource MahApps.Brushes.Control.Border}"/>

                            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                                <iconPacks:PackIconMaterial Kind="DatabaseCog" Foreground="#1E40AF" Width="24" Height="24" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <TextBlock Text="Управление базой данных (Режим разработчика)" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                            </StackPanel>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="30"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0" Background="#F0FDF4" BorderBrush="#BBF7D0" BorderThickness="1" CornerRadius="8" Padding="20">
                                    <StackPanel>
                                        <TextBlock Text="Генератор тестовых данных" FontSize="16" FontWeight="SemiBold" Foreground="#166534" Margin="0,0,0,15"/>
                                        <TextBlock Text="Автоматически создаст реалистичные записи контрагентов, автопарка и номенклатуры для тестирования производительности." TextWrapping="Wrap" Foreground="#15803D" Margin="0,0,0,20"/>

                                        <TextBlock Text="Какой справочник заполнить:" FontWeight="SemiBold" Foreground="#166534" Margin="0,0,0,8"/>
                                        <ComboBox ItemsSource="{Binding AvailableDictionaries}" SelectedItem="{Binding SelectedDictionary}" Height="40" FontSize="15" Margin="0,0,0,15"/>

                                        <TextBlock Text="Количество записей:" FontWeight="SemiBold" Foreground="#166534" Margin="0,0,0,8"/>
                                        <ComboBox ItemsSource="{Binding AvailableGenerateCounts}" SelectedItem="{Binding GenerateCount}" Height="40" FontSize="15" Margin="0,0,0,20"/>

                                        <Button Command="{Binding GenerateTestDataCommand}" Style="{StaticResource PrimaryButton}" Background="#16A34A" BorderBrush="#16A34A" Height="42">
                                            <StackPanel Orientation="Horizontal">
                                                <iconPacks:PackIconMaterial Kind="DatabasePlus" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                                <TextBlock Text="СГЕНЕРИРОВАТЬ ДАННЫЕ" VerticalAlignment="Center" FontWeight="Bold"/>
                                            </StackPanel>
                                        </Button>
                                    </StackPanel>
                                </Border>

                                <Border Grid.Column="2" Background="#FEF2F2" BorderBrush="#FECACA" BorderThickness="1" CornerRadius="8" Padding="20">
                                    <StackPanel>
                                        <TextBlock Text="Полный сброс базы данных" FontSize="16" FontWeight="SemiBold" Foreground="#991B1B" Margin="0,0,0,15"/>
                                        <TextBlock Text="ВНИМАНИЕ! Это действие безвозвратно удалит все документы, справочники и движения. Аккаунты пользователей и роли будут сохранены." TextWrapping="Wrap" Foreground="#B91C1C" FontWeight="SemiBold" Margin="0,0,0,20"/>

                                        <TextBlock Text="Пароль администратора для подтверждения:" FontWeight="SemiBold" Foreground="#991B1B" Margin="0,0,0,8"/>
                                        <PasswordBox x:Name="WipePasswordBox" Height="40" FontSize="15" Margin="0,0,0,20">
                                            <PasswordBox.Style>
                                                <Style TargetType="PasswordBox">
                                                    <Setter Property="Padding" Value="10,8"/>
                                                    <Setter Property="BorderThickness" Value="1"/>
                                                    <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                                                    <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
                                                    <Setter Property="FontSize" Value="14"/>
                                                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="PasswordBox">
                                                                <Border x:Name="border"
                                                                        Background="{TemplateBinding Background}"
                                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                                        CornerRadius="4">
                                                                    <ScrollViewer x:Name="PART_ContentHost" Margin="0"/>
                                                                </Border>
                                                                <ControlTemplate.Triggers>
                                                                    <Trigger Property="IsFocused" Value="True">
                                                                        <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                                                        <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                                                                    </Trigger>
                                                                </ControlTemplate.Triggers>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Style>
                                            </PasswordBox.Style>
                                        </PasswordBox>

                                        <Button Command="{Binding WipeDatabaseCommand}" CommandParameter="{Binding ElementName=WipePasswordBox}" Style="{StaticResource PrimaryButton}" Background="#DC2626" BorderBrush="#DC2626" Height="42">
                                            <StackPanel Orientation="Horizontal">
                                                <iconPacks:PackIconMaterial Kind="DatabaseRemove" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                                <TextBlock Text="УНИЧТОЖИТЬ ВСЕ ДАННЫЕ" VerticalAlignment="Center" FontWeight="Bold"/>
                                            </StackPanel>
                                        </Button>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>
            </TabControl>
        </Border>
    </Grid>
</UserControl>