<mah:MetroWindow x:Class="LogisticsApp.Views.Windows.InventoryDocumentEditWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                 xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
                 xmlns:core="clr-namespace:LogisticsApp.Core"
                 Title="Документ движения товаров"
                 Height="650" Width="950"
                 WindowStartupLocation="CenterScreen"
                 Background="{StaticResource BackgroundBrush}"
                 GlowBrush="{StaticResource PrimaryBrush}"
                 BorderThickness="1">

    <mah:MetroWindow.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <core:InventoryDocumentTypeToTextConverter x:Key="InventoryDocumentTypeToTextConverter"/>
    </mah:MetroWindow.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="15" IsEnabled="{Binding IsEditable}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="20" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Margin="0,0,0,15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Дата:" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,10,15"/>
                    <DatePicker Grid.Row="0" Grid.Column="1" SelectedDate="{Binding DocumentDate}" Margin="0,0,20,15" Height="35"/>

                    <TextBlock Grid.Row="0" Grid.Column="2" Text="Склад:" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,10,15"/>
                    <ComboBox Grid.Row="0" Grid.Column="3" ItemsSource="{Binding AvailableWarehouses}" SelectedItem="{Binding SelectedWarehouse}" DisplayMemberPath="Name" Margin="0,0,0,15" Height="35"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Операция:" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <ComboBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding AvailableDocumentTypes}" SelectedItem="{Binding Type}" Margin="0,0,20,0" Height="35">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Converter={StaticResource InventoryDocumentTypeToTextConverter}}"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>

                    <TextBlock Grid.Row="1" Grid.Column="2" Text="Основание:" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBox Grid.Row="1" Grid.Column="3" Text="{Binding Reason, UpdateSourceTrigger=PropertyChanged}" Height="35"/>
                </Grid>
            </Border>

            <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="15" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                            <Button Content="+ ДОБАВИТЬ" Command="{Binding AddItemCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,10,0"/>
                            <Button Content="УДАЛИТЬ" Command="{Binding RemoveItemCommand}" Style="{StaticResource MenuButton}" Foreground="#D32F2F"/>
                        </StackPanel>

                        <!-- БЛОК РЕДАКТИРОВАНИЯ: Теперь с поддержкой Упаковок/ВГХ -->
                        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SelectedItem}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>

                            <TextBlock Text="{Binding SelectedItem.Product.Name}" MaxWidth="220" TextTrimming="CharacterEllipsis" FontSize="14" FontWeight="Bold" Margin="0,0,15,0" VerticalAlignment="Center" Foreground="{StaticResource TextPrimaryBrush}"/>

                            <TextBlock Text="Ед. изм.:" FontSize="14" FontWeight="SemiBold" Margin="0,0,10,0" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}"/>
                            <ComboBox ItemsSource="{Binding SelectedItem.AvailablePackagings}"
                                      SelectedItem="{Binding SelectedItem.SelectedPackaging, UpdateSourceTrigger=PropertyChanged}"
                                      DisplayMemberPath="Unit.Name"
                                      Width="100" Margin="0,0,15,0" Height="36"
                                      IsEnabled="{Binding IsEditable}"
                                      Style="{StaticResource MahApps.Styles.ComboBox}"/>

                            <TextBlock Text="Кол-во:" FontSize="14" FontWeight="SemiBold" Margin="0,0,10,0" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" IsEnabled="{Binding IsEditable}">
                                <Button Command="{Binding SelectedItem.DecreaseQuantityCommand}" Content="-" Width="36" Height="36" Padding="0" Style="{StaticResource MahApps.Styles.Button.Square.Accent}" Background="White" Foreground="Black" BorderBrush="#CBD5E1" BorderThickness="1,1,0,1" FontSize="18"/>
                                <TextBox Text="{Binding SelectedItem.Quantity, UpdateSourceTrigger=PropertyChanged}" Width="60" Height="36" TextAlignment="Center" VerticalContentAlignment="Center" FontWeight="Bold" FontSize="15" BorderBrush="#CBD5E1" BorderThickness="1"/>
                                <Button Command="{Binding SelectedItem.IncreaseQuantityCommand}" Content="+" Width="36" Height="36" Padding="0" Style="{StaticResource MahApps.Styles.Button.Square.Accent}" Background="White" Foreground="Black" BorderBrush="#CBD5E1" BorderThickness="0,1,1,1" FontSize="18"/>
                            </StackPanel>
                        </StackPanel>
                    </Grid>

                    <DataGrid Grid.Row="1" ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem}" AutoGenerateColumns="False" CanUserAddRows="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Артикул" Binding="{Binding Product.SKU}" IsReadOnly="True" Width="120" FontWeight="Bold"/>
                            <DataGridTextColumn Header="Номенклатура" Binding="{Binding Product.Name}" IsReadOnly="True" Width="*"/>
                            <DataGridTextColumn Header="Упаковка" Binding="{Binding SelectedPackaging.Unit.Name}" IsReadOnly="True" Width="100"/>
                            <DataGridTextColumn Header="Количество" Binding="{Binding Quantity}" IsReadOnly="True" Width="100" FontWeight="Bold"/>
                            <!-- Информационная колонка: показывает, сколько это в базовых единицах (Quantity * Coefficient) -->
                            <DataGridTextColumn Header="Итого (баз. ед.)" Binding="{Binding TotalBaseQuantity}" IsReadOnly="True" Width="120" Foreground="{StaticResource TextSecondaryBrush}" FontStyle="Italic"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Padding="20,15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="ЗАКРЫТЬ" Command="{Binding CancelCommand}" Style="{StaticResource MenuButton}" Margin="0,0,15,0" FontWeight="SemiBold"/>

                <Button Content="ОТМЕНИТЬ ПРОВЕДЕНИЕ" Command="{Binding UnpostCommand}" Style="{StaticResource MenuButton}" Foreground="#F59E0B" Margin="0,0,15,0" Visibility="{Binding IsPosted, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                <Button Content="ОПРИХОДОВАТЬ ПО ЗАЯВКЕ" Command="{Binding FulfillRequestCommand}" Style="{StaticResource PrimaryButton}" Background="#10B981" Width="240" Margin="0,0,15,0" Visibility="{Binding IsRequest, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                <Button Content="СОХРАНИТЬ ЧЕРНОВИК" Command="{Binding SaveDraftCommand}" Style="{StaticResource MenuButton}" Margin="0,0,15,0" Visibility="{Binding IsEditable, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                <Button Content="ПРОВЕСТИ ДОКУМЕНТ" Command="{Binding PostCommand}" Style="{StaticResource PrimaryButton}" Width="200" Visibility="{Binding IsStandardEditable, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </StackPanel>
        </Border>

        <Border Grid.RowSpan="2" Background="#80FFFFFF" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <mah:ProgressRing IsActive="True" Foreground="{StaticResource PrimaryBrush}" Width="50" Height="50"/>
        </Border>
    </Grid>
</mah:MetroWindow>